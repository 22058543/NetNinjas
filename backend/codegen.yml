schema: ./graphql/schema.json
documents:
  - ./graphql/operations/**/*.graphql
generates:
  ./src/graphql/types.ts:
    plugins:
      - typescript
      - typescript-operations
config:
  immutableTypes: true
  enumsAsTypes: true
